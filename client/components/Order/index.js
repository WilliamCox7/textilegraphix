import { React, Component } from '../../packages';
import './style.scss';

class Order extends Component {
  render() {
    return (
      <div className="Order">
        Order
      </div>
    );
  }
}

export default Order;
